# FOLIO

Slides inspired from the Lion's Commentary on the UNIX Operating System.

## Features

- Solutions boxes with `#solution[]` that can be showed or hidden by passing `--input solutions=true` or `#show folio.with(..., show_solutions: true)`.

## Fonts

The template use _Futura_ as fonts. Those can be found in the `assets/` folder.